{
  "scripts": {
    "heroku-prebuild": "cd frontend/ && yarn install && yarn build",
    "heroku-postbuild": "cd ../"
  },
  "engines": {
      "node": "9.11.1",
      "npm": "5.6.0",
      "yarn": "1.6.0"
    }
}
